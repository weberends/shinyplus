% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plus_remote_functions.R
\name{plus_remote_functions}
\alias{plus_remote_functions}
\alias{plus_login}
\alias{plus_logout}
\alias{plus_add_products}
\alias{plus_open_browser}
\alias{plus_current_cart}
\alias{plus_checkout}
\alias{plus_current_url}
\alias{plus_page_reload}
\title{Interact with PLUS.nl via Headless Browser}
\usage{
plus_login(
  credentials = getOption("plus_credentials", default =
    system.file("login_credentials.yaml", package = "shinyplus")),
  info = interactive()
)

plus_logout(info = interactive())

plus_add_products(x, quantity = 1, info = interactive(), ...)

plus_current_cart(..., info = interactive())

plus_checkout()

plus_current_url(b = plus_env$browser)

plus_open_browser()

plus_page_reload()
}
\arguments{
\item{credentials}{Path to a YAML file containing fields \code{email} and \code{password}, or a \link{list} contains those names. Can be set with \code{options(plus_credentials = "...")}}

\item{info}{Logical to print info, default is \code{TRUE} in interactive sessions.}

\item{x}{Name of the product, such as "PLUS Houdbare Halfvolle Melk Pak 1000 ml", or URL of the product, such as "plus-houdbare-halfvolle-melk-pak-1000-ml-957806".}

\item{quantity}{Number of items to add to cart.}

\item{...}{arguments passed to \code{\link[=plus_login]{plus_login()}}.}

\item{b}{Browser to use.}
}
\description{
Provides functions to log in, add products to the cart, retrieve the current cart, and navigate the \href{https://www.plus.nl}{PLUS.nl website} using a \code{\link[chromote:Chromote]{Chromote}} browser session.
}
\section{Disclaimer}{



This open-source R package, \emph{shinyplus}, is an independent software project and is not affiliated with, endorsed by, or associated in any way with PLUS Retail B.V. or the website \href{https://www.plus.nl}{plus.nl}.

The package does \strong{not} contain or store any data from plus.nl. It contains \strong{no product information, pricing data, or content} originating from PLUS Retail B.V. or its systems. Users are required to provide their own data (e.g., product lists) manually.

All interactions with plus.nl occur through \strong{local, client-side browser automation}, using the \href{https://rstudio.github.io/chromote/}{\code{chromote}} package. This mechanism emulates a user operating a private browser session on their own machine. For example, when a user submits a basket to their PLUS shopping cart, this package simply opens a browser in the background and automates user-visible steps — such as logging in, navigating to product pages, and clicking buttons — exactly as a person would. No API calls or server-side data access is used or implied.

Any logos, icons, or brand references (e.g., the PLUS logo) used in the user interface are publicly available media assets sourced from \href{https://nl.wikipedia.org/wiki/PLUS_(Nederlandse_supermarkt)}{Wikipedia} under appropriate free-use licensing. All trademarks, service marks, and product names are the property of their respective owners.
}

\examples{
\dontrun{
plus_login()

plus_add_products("Houdbare halfvolle melk pak 1000 ml")
}
}
